#include<iostream>
using namespace std;
int main()
{

  enum weekday {monday, tuesday, wednesday, thursday, friday, saturday,   sunday}; 
    weekday today = monday; 
    weekday tomorow = tuesday; 
      cout<<today<<"  "<<tomorow<<endl;
       return 0;
}